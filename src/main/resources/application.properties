spring.application.name=lms

# --- PostgreSQL datasource (adjust host/port/db/user/pass as needed) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/lms
spring.datasource.username=postgres
spring.datasource.password=Pilipic10$$$
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Flyway ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.schemas=public

# --- Hibernate / JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# (optional) timezone consistency
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# --- App JWT ---
# MUST be at least 32 characters
app.jwt.secret=j430tj340utijfsifjgsoijjOIJXIWJXoiaj9019032i12keopkakwdwaopdk1i4912iopkqwodpwqk91'2i34
app.jwt.issuer=lms
app.jwt.access-token-minutes=30

# --- Mail / notifications (adjust for your SMTP server) ---
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

app.notifications.from-address=no-reply@lms.local

# --- Scheduler ---
# How often to check for upcoming assignment deadlines
app.scheduler.assignment-check-fixed-delay-ms=20000
# How many hours before deadline to send reminders
app.scheduler.reminder-hours-before-deadline=720
